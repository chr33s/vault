{
	"name": "vault-relay-worker",
	"version": "0.1.0",
	"private": true,
	"description": "Serverless vault relay (Cloudflare Worker + Durable Object). Zero runtime dependencies.",
	"type": "module",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy"
	},
	"devDependencies": {
		"wrangler": "3.90.0"
	}
}
